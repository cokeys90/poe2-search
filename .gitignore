node_modules
dist
.idea
.firebase
*.log
.DS_Store
.env
.env.local

# Claude Code 설정·지침서 (개인용, 공개 안 함)
CLAUDE.md
.claude/

# 개인 메모·데이터 (공개 안 함)
# 루트 고정(/) — 그냥 data/ 로 두면 src/data/ 까지 무시돼 앱 데이터가 커밋에서 빠진다
/memo/
/data/
# 수집·생성 중간 산출물 (스크립트로 재생성 가능)
scripts/out/
